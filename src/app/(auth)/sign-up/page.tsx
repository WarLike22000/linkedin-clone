import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign Up",
};

const SignUp = () => {
  return (
    <main className="flex h-screen items-center justify-center p-5"></main>
  );
};

export default SignUp;
